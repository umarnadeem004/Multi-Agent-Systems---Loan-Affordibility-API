services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: affordability
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - affordability_postgres_data:/var/lib/postgresql/data

volumes:
  affordability_postgres_data: